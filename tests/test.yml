# test file for new-relic-servers
---
- hosts: localhost
  connection: local
  sudo: true
  pre_tasks:
    - debug:
        msg: "{{ iten }}"
      with_items:
        - "{{ lookup('env', 'NEW_RELIC_LICENSE_KEY1') }}"
        - "{{ lookup('env', 'NEW_RELIC_LICENSE_KEY2') }}"

  roles:
    - ../../
  vars:
    new_relic_servers_license_key: "{{ lookup('env', 'NEW_RELIC_LICENSE_KEY') }}"
